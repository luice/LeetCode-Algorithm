## 题目

给一个整数数组和一个目标值，返回一个数组，表示原数组中的两个数的下标，使得这两个数相加等于给定的目标值。

可以假设每个输入有且只有一个解决方案，并且不会两次使用相同的元素。

例：

给定 nums = [2, 7, 11, 15], target = 9，

因为 nums[0] + nums[1] = 2 + 7 = 9，
返回 [0, 1]。

## 解题思路

给定的数组没有任何特征，所以没什么好说的，直接暴搜。

## 运行结果

27ms，性能超过了 34.01% 的 Java 提交。